import React from "react";
import TokenLockComponent from "../../components/CryptoTokenLockComponent";

const TokenLockContainer = (props) => {
  return (
    <>
      <TokenLockComponent {...props} />
    </>
  );
};

export default TokenLockContainer;
