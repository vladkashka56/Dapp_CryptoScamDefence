import React from "react";
import CryptoTeamComponent from "../../components/CryptoTeamComponent";

const TeamContainer = (props) => {
  return (
    <>
      <CryptoTeamComponent {...props} />
    </>
  );
};

export default TeamContainer;
